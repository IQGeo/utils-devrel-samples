import React from 'react';
import Selector from './selector/selector';

class StructureSelector extends React.Component {
    render() {
        return <Selector props={this.props} />;
    }
}

export default StructureSelector;
